# --- Core Dependencies ---
pandas>=1.3.0             # 데이터 조작 및 분석의 핵심 라이브러리
openpyxl>=3.0.10          # .xlsx 엑셀 파일을 읽고 쓰기 위한 엔진

# --- CLI Interface ---
typer>=0.9.0              # 현대적이고 사용하기 쉬운 CLI 애플리케이션 빌더
click>=8.0.0              # Typer의 기반이 되는 라이브러리
rich>=13.0.0              # 터미널에 예쁘고 다채로운 텍스트를 출력 (선택 사항)

# --- Utilities ---
chardet>=5.0.0            # 파일의 문자 인코딩(cp949, utf-8 등)을 자동으로 감지
charset-normalizer>=3.0.0 # chardet과 유사한 인코딩 감지 라이브러리