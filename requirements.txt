# Runtime dependencies for PyNorma.
#
# pyproject.toml is the authoritative source; this file mirrors the *core*
# runtime dependencies for `pip install -r requirements.txt` workflows.
#
# Optional extras (install from the project root):
#   pip install ".[cli]"   # command-line interface: typer, click, rich
#   pip install ".[dev]"   # tests & lint: pytest, pytest-cov, ruff

pandas>=2.0        # DataFrame engine
openpyxl>=3.1      # .xlsx reading/writing
chardet>=5.0       # character-encoding detection (cp949, utf-8, …)
